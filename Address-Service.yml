test.message: "Hello from config server for Address Service"

management:
  endpoints:
    web:
      exposure:
        include: refresh

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

  instance:
    prefer-ip-address: true
